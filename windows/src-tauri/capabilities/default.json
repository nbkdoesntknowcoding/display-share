{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the receiver actually uses. Tauri v2 denies any plugin command not listed here, at runtime — so an omission here looks like a silent no-op, not a build error.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-fullscreen",
    "updater:default",
    "process:allow-restart"
  ]
}
