.DS_Store

# Swift / SwiftPM
.build/
# xcodebuild's default output location when no -derivedDataPath is given.
mac/build/
.swiftpm/
*.xcuserstate
xcuserdata/
DerivedData/

# Spike output
spike-frames/
*.h264
*.mov

# Node / Tauri (Phase 3)
node_modules/
dist/
windows/src-tauri/target/

# Secrets — signing identities, notarization creds, updater keys
*.p12
*.cer
*.provisionprofile
.env
.env.*
